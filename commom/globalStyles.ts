// src/styles/globalStyles.ts

import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  lightBackground: {
    flex: 1,
    backgroundColor: 'white',
    // Outros estilos globais para o tema light aqui
  },
  darkBackground: {
    flex: 1,
    backgroundColor: 'black',
    // Outros estilos globais para o tema dark aqui
  },
  // Outros estilos globais comuns a ambos os temas aqui
});
